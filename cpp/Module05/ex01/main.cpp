#include "Form.h"

int main(void) {
	/*
	try {
		Bureaucrat jerk("fucker", 0);
		jerk.checkGrade(1, 150);
	}
	catch (const std::exception& e) {
        std::cout << "Bureaucrat " << e.what() <<  "\n";
    }
	*/
	return (0);
}
